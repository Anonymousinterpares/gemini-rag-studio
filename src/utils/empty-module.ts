// Empty stub module — used to silence `Dynamic require of "fs"` in browser/Electron renderer environments.
// Xenova/transformers loads models over HTTP, so it never actually needs Node's fs in the browser context.
export default {};
